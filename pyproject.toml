[build-system]
requires = ["setuptools>=42","wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "interconnector-flow-model"
version = "0.0.0"
description = "Forecasting scaffold for interconnector flows"
readme = "README.md"

[tool.pytest]
minversion = "6.0"
